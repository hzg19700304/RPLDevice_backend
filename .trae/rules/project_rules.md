不要随意新建测试脚本，尽量使用main_server.py作为测试入口
如果要生成测试文件，请将测试文件放在tests目录下
编写代码之前先查询项目根目录下docs目录下的相关文档，确认需求是否已经实现
不要过度设计，只实现必要的功能
尽量不使用硬编码，而是使用配置文件或环境变量等方式来存储配置信息
所有的配置均放在config目录下的config.ini文件中
安装依赖时必须先确认是否虚拟环境已激活